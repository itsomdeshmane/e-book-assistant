# =============================================================================
# DEPENDENCY VERSIONS
# =============================================================================
# Python 3.13+ compatible versions
# ChromaDB 0.5.5+ now supports NumPy 2.x
# PyTorch CPU-only version is used to minimize package size
# =============================================================================

# Scientific Computing & Image Processing
# NumPy 2.x is compatible with ChromaDB 0.5.5+ and required for Python 3.13+
# OpenCV requires numpy<2.3.0, so we cap at 2.2.x
numpy>=2.0,<2.3

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# PDF Processing
pypdf==4.3.1
pdf2image==1.17.0
pillow==10.4.0
opencv-python-headless==4.10.0.84
pdfplumber==0.11.4
pymupdf>=1.23.0  # PDF rendering fallback (fitz)

# Vector Database & AI
# Using Pinecone for cloud-based vector storage
# OpenAI for embeddings (mandatory)
pinecone>=5.0.0
openai==1.51.0

# Database & ORM
sqlmodel==0.0.22

# Authentication & Security
passlib[bcrypt]==1.7.4
argon2-cffi==23.1.0
python-jose[cryptography]==3.3.0

# Configuration & Environment
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12

# Azure AI Document Intelligence SDK
azure-ai-documentintelligence==1.0.0b4

# HTTP & Utilities
requests==2.32.3
email-validator==2.2.0
python-magic==0.4.27
psutil==6.0.0

# Additional OpenCV
opencv-python==4.10.0.84
